/**
 * Función que es llamado desde el modulo de gwt luego que el modulo sea cargado
 * Gattex
 */
function gwtOnReady() {
	onReadyMain();
};

function closePopupInfo(popupId){
	$('.icon-remove-sign').click();
}
